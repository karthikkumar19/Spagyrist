class Profile {
    constructor(id,firstName,lastName,email,mobile,dob,address){
        this.id = id;
       this.firstName = firstName,
       this.lastName = lastName,
       this.email = email,
       this.mobile = mobile,
       this.dob = dob,
       this.address = address
    }
}

export default Profile;