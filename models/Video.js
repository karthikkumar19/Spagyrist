class Video{
    constructor(nid,title,thumbnail_image,category,length,video_url){
            this.nid = nid;
            this.title = title;
            this.thumbnail_image = thumbnail_image;
            this.category=category;
            this.length = length;
            this.video_url = video_url;
    }
}

export default Video;
